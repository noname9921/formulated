var e=`# The Smith Normal Form and Rational Canonical Form

## Definition

The Smith Normal Form (SNF) and the Rational Canonical Form (RCF) are two cornerstone concepts in linear algebra and module theory, primarily concerned with the classification of matrices and linear operators over principal ideal domains (PIDs).

The Smith Normal Form is a canonical form for matrices with entries in a PID, such as the ring of integers $\\mathbb{Z}$ or the polynomial ring $F[x]$ over a field $F$. Given an $m \\times n$ matrix $A$ over a PID $R$, there exist invertible matrices $P \\in GL_m(R)$ and $Q \\in GL_n(R)$ such that the product $D = PAQ$ is a diagonal matrix of the form:
$$D = \\text{diag}(d_1, d_2, \\dots, d_k, 0, \\dots, 0)$$
where the diagonal elements $d_i$ satisfy the divisibility condition $d_1 | d_2 | \\dots | d_k$. These elements $d_i$ are called the invariant factors of $A$, and they are unique up to multiplication by a unit in $R$.

The Rational Canonical Form, also known as the Frobenius normal form, is a canonical form for linear operators $T: V \\to V$ on a finite-dimensional vector space $V$ over a field $F$. For a matrix $A \\in M_n(F)$, the RCF is a block diagonal matrix $C$ obtained via similarity transformations, such that $C = P^{-1}AP$. The blocks are companion matrices $C(f_i)$ associated with the invariant factors $f_i(x) \\in F[x]$ of the characteristic module of the operator. Specifically, the RCF is the matrix:
$$C = \\begin{pmatrix} C(f_1) & 0 & \\dots & 0 \\\\ 0 & C(f_2) & \\dots & 0 \\\\ \\vdots & \\vdots & \\ddots & \\vdots \\\\ 0 & 0 & \\dots & C(f_m) \\end{pmatrix}$$
where $f_1 | f_2 | \\dots | f_m$ and $f_1 f_2 \\dots f_m$ is the minimal polynomial of $A$.

## Key Terminology

To navigate these concepts, one must understand several technical terms:

1. Principal Ideal Domain (PID): An integral domain where every ideal is principal (generated by a single element). Common examples include $\\mathbb{Z}$ and $F[x]$.
2. Invariant Factors: The diagonal elements $d_i$ in the Smith Normal Form. They contain complete structural information about the module or operator being analyzed.
3. Companion Matrix: A matrix $C(p)$ associated with a monic polynomial $p(x) = x^k + a_{k-1}x^{k-1} + \\dots + a_1x + a_0$. It has 1s on the subdiagonal and the coefficients of the polynomial in the last column.
4. Characteristic Polynomial: Defined as $\\det(xI - A)$, it provides the eigenvalues of a matrix.
5. Minimal Polynomial: The unique monic polynomial of smallest degree that annihilates the matrix $A$.
6. Similarity: Two matrices $A$ and $B$ are similar if there exists an invertible matrix $P$ such that $B = P^{-1}AP$. Similarity preserves the RCF.
7. Equivalence: Two matrices $A$ and $B$ are equivalent if $B = PAQ$ for invertible $P$ and $Q$. SNF characterizes equivalence classes of matrices over a PID.

## Purpose

The primary utility of these forms is the classification of mathematical structures. In linear algebra, the problem of determining whether two matrices are similar is non-trivial. While the Jordan Canonical Form provides a classification over algebraically closed fields, it fails if the eigenvalues are not in the base field. The Rational Canonical Form provides a solution that works over any field $F$ without requiring the extension of scalars.

The Smith Normal Form acts as a bridge. By viewing a matrix $A$ over $F[x]$ as the matrix $(xI - A)$, the SNF of this matrix yields the invariant factors of the operator $A$. These invariant factors are fundamental invariants; they determine the structure of the module $V$ as a direct sum of cyclic modules. Furthermore, in integer matrix theory, SNF is used to find the structure of finitely generated abelian groups. If a group is defined by a presentation matrix of relations, the SNF of that matrix reveals the group's decomposition into cyclic components, facilitating the computation of group properties like torsion and rank.

## Fundamental Properties

The power of these forms lies in their uniqueness and their ability to capture underlying structural invariants.

For SNF, if $A$ is an $m \\times n$ matrix over a PID $R$, the invariant factors $d_i$ are uniquely determined. They are computed using determinantal divisors $\\delta_k(A)$, which are the greatest common divisors of all $k \\times k$ minors of $A$. Specifically, $d_k = \\delta_k / \\delta_{k-1}$ (with $\\delta_0 = 1$). This implies that the SNF is an absolute classifier for matrix equivalence.

For RCF, the invariant factors $f_i(x)$ of an operator $T$ have a direct relationship with the matrix structure. The largest invariant factor $f_m(x)$ is the minimal polynomial of $A$. The product of all invariant factors $\\prod f_i(x)$ is the characteristic polynomial of $A$. Because RCF is unique, two matrices are similar over $F$ if and only if they have the same RCF. This is a powerful result, as it allows for the definitive testing of matrix similarity using only rational operations, avoiding the need to find roots of polynomials in potentially complex algebraic closures.

## Types & Variations

While the basic definitions are robust, variations appear depending on the underlying structure:

1. Smith Normal Form over $\\mathbb{Z}$: Used to compute the structure of finitely generated abelian groups. If $A$ is a relation matrix, the group $G \\cong \\mathbb{Z}^n / \\text{im}(A)$ decomposes into $\\mathbb{Z}^k \\oplus \\mathbb{Z}/d_1\\mathbb{Z} \\oplus \\dots \\oplus \\mathbb{Z}/d_m\\mathbb{Z}$.
2. Smith Normal Form over $F[x]$: The core of the RCF derivation. It treats the matrix $(xI - A)$ as an element of the module of matrices over the polynomial ring.
3. Rational Canonical Form vs. Jordan Canonical Form: The RCF is essentially "field-agnostic," relying only on the base field $F$. In contrast, the Jordan form requires the eigenvalues to exist in $F$. If the characteristic polynomial splits completely in $F$, the RCF is closely related to the Jordan form, but the RCF remains the standard for general fields.
4. Smith Normal Form of Operators: One can define SNF for operators on modules, though this requires the PID structure to handle the module's torsion and free parts.

## How to Solve

Computing the Smith Normal Form and the Rational Canonical Form involves systematic reduction.

### Computing Smith Normal Form (via elementary operations)
The SNF is reached by applying elementary row and column operations:
1. Swapping rows/columns.
2. Multiplying a row/column by a unit (e.g., $-1$ in $\\mathbb{Z}$, non-zero constant in $F$).
3. Adding a multiple of one row/column to another.

The algorithm proceeds by:
- Selecting the non-zero entry with the smallest "size" (e.g., smallest absolute value or degree).
- Using Euclidean division to eliminate other entries in the same row and column.
- Ensuring the divisibility condition $d_i | d_{i+1}$ through additional row/column manipulations.
- Repeating until only diagonal entries remain.

### Computing Rational Canonical Form
To find the RCF of a matrix $A$:
1. Form the matrix $xI - A$ with entries in $F[x]$.
2. Compute the Smith Normal Form of $xI - A$ using the algorithm above.
3. The diagonal entries $d_i(x)$ are the invariant factors of $A$. Note that units in $F[x]$ are non-zero constants; normalize $d_i(x)$ to be monic.
4. Construct the companion matrix $C(f_i)$ for each invariant factor $f_i(x)$.
5. Arrange these companion matrices as diagonal blocks in the final RCF matrix.

This method transforms a complicated similarity problem into a computational exercise of polynomial manipulation. For example, if $f(x) = x^3 + a_2x^2 + a_1x + a_0$, the companion matrix is:
$$C(f) = \\begin{pmatrix} 0 & 0 & -a_0 \\\\ 1 & 0 & -a_1 \\\\ 0 & 1 & -a_2 \\end{pmatrix}$$

## Summary

The Smith Normal Form and Rational Canonical Form represent the pinnacle of structural classification in matrix theory. By reducing matrices to canonical forms using either elementary operations or polynomial invariants, mathematicians gain deep insight into the internal properties of linear transformations and modules.

The SNF provides a universal mechanism to handle matrices over PIDs, offering a way to decompose modules and identify the structure of abelian groups. Simultaneously, the RCF offers a field-independent methodology for checking matrix similarity, ensuring that the properties of an operator are correctly identified regardless of the existence of eigenvalues in the base field. Mastery of these two forms is essential for advanced work in algebra, control theory, and number theory, as they provide the precise technical language required to describe how complex systems can be reduced to their most fundamental, irreducible components.

| Form | Domain | Primary Utility | Invariants |
| :--- | :--- | :--- | :--- |
| SNF | PID ($R$) | Matrix Equivalence | Invariant factors $d_i$ |
| RCF | Field ($F$) | Matrix Similarity | Invariant polynomials $f_i(x)$ |
| SNF | $F[x]$ | Basis of RCF | Polynomial factors $d_i(x)$ |
| RCF | $\\mathbb{Q}$ or $\\mathbb{R}$ | Structural Analysis | Minimal polynomial |`;export{e as default};