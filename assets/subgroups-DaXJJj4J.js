var e=`# Subgroups and Centralizers

## Definition

In abstract algebra, particularly group theory, a subgroup $H$ of a group $G$ is a subset $H \\subseteq G$ that is itself a group under the binary operation inherited from $G$. For $H$ to be a subgroup, it must satisfy three conditions: it must contain the identity element $e$ of $G$, it must be closed under the group operation (if $a, b \\in H$, then $ab \\in H$), and it must be closed under inversion (if $a \\in H$, then $a^{-1} \\in H$).

The centralizer of an element $g \\in G$, denoted as $C_G(g)$, is defined as the set of all elements in $G$ that commute with $g$:
$$C_G(g)=\\{x \\in G \\mid xg=gx\\}$$
Equivalently, this can be written using the commutator notation as $C_G(g)=\\{x \\in G \\mid xgx^{-1}=g\\}$. The centralizer is a specific type of subgroup. More generally, for any subset $S \\subseteq G$, the centralizer of $S$ is defined as $C_G(S)=\\{x \\in G \\mid xs=sx \\text{ for all } s \\in S\\}$. The centralizer of the entire group $G$ is known as the center of the group, denoted $Z(G)$, which consists of all elements that commute with every element in the group.

## Key Terminology

- **Group ($G$):** A set equipped with a binary operation that satisfies closure, associativity, identity, and invertibility.
- **Subgroup ($H \\leq G$):** A subset of $G$ that forms a group under the operation restricted from $G$.
- **Commutativity:** A property where two elements $a$ and $b$ commute if $ab=ba$.
- **Centralizer ($C_G(S)$):** The set of elements that commute with every element in a subset $S$ of $G$.
- **Normalizer ($N_G(H)$):** Given a subgroup $H$, the normalizer is the set $\\{g \\in G \\mid gHg^{-1}=H\\}$. It is the largest subgroup of $G$ in which $H$ is normal.
- **Center ($Z(G)$):** The set of elements that commute with everything in $G$. Formally, $Z(G)=C_G(G)$.
- **Index:** For a subgroup $H \\leq G$, the index $[G:H]$ is the number of left cosets of $H$ in $G$.
- **Abelian Group:** A group where $ab=ba$ for all $a, b \\in G$. In an abelian group, $C_G(g)=G$ for all $g \\in G$.

## Purpose

The study of subgroups and centralizers is fundamental to understanding the structural decomposition of groups. Subgroups allow us to view a large, complex group as being composed of smaller, more manageable blocks. For instance, Lagrange's Theorem dictates that the order of a subgroup must divide the order of the group, providing a powerful constraint on the structure of finite groups.

Centralizers serve as a measure of "how far" a group is from being abelian. By partitioning the group into centralizers, one can analyze the symmetry of individual elements. If $C_G(g)$ is large, $g$ behaves almost like a central element. Centralizers play a critical role in the Class Equation, which is a major tool for classifying finite groups. The Class Equation is given by:
$$|G|=|Z(G)|+\\sum_{i=1}^k [G:C_G(g_i)]$$
where $g_i$ are representatives of the non-central conjugacy classes. This equation bridges the gap between the global structure of $G$ and the local behavior of its centralizers.

## Fundamental Properties

Subgroups and centralizers satisfy several rigorous mathematical properties that permit deep analysis of algebraic structures.

1. **Subgroup Criteria:** A non-empty subset $H$ is a subgroup if and only if $ab^{-1} \\in H$ for all $a, b \\in H$.
2. **Centralizers are Subgroups:** For any $S \\subseteq G$, $C_G(S)$ is always a subgroup of $G$. This is verified by noting that the identity commutes with everything, and if $x, y \\in C_G(S)$, then $(xy)s=x(ys)=x(sy)=(xs)y=(sx)y=s(xy)$, so $xy \\in C_G(S)$.
3. **Inclusion-Reversal:** If $S \\subseteq T \\subseteq G$, then $C_G(T) \\subseteq C_G(S)$. As we add more elements to the set we want to commute with, the centralizer must shrink.
4. **Relationship to Center:** $Z(G) \\subseteq C_G(S)$ for any $S$. The center is the intersection of all centralizers of individual elements.
5. **Lagrange's Theorem:** If $G$ is a finite group and $H \\leq G$, then $|H|$ divides $|G|$. Consequently, for any $g \\in G$, $|C_G(g)|$ must divide $|G|$.
6. **Conjugacy:** The size of the conjugacy class of $g$, denoted $Cl(g)$, is exactly the index of its centralizer: $|Cl(g)|=[G:C_G(g)]$. This shows that elements with large centralizers have small conjugacy classes.

## Types & Variations

There are several ways to categorize subgroups and centralizers based on their algebraic properties:

| Subgroup Type | Description |
|:---|:---|
| Trivial Subgroup | The subgroup consisting only of the identity element $\\{e\\}$. |
| Improper Subgroup | The entire group $G$ itself. |
| Normal Subgroup | A subgroup $N$ such that $gNg^{-1}=N$ for all $g \\in G$. |
| Cyclic Subgroup | A subgroup generated by a single element, $\\langle a \\rangle = \\{a^n \\mid n \\in \\mathbb{Z}\\}$. |
| Sylow Subgroup | If $|G|=p^n m$ where $p \\nmid m$, a subgroup of order $p^n$ is a Sylow $p$-subgroup. |

Centralizers also vary depending on the subset $S$:
- **Centralizer of an element ($C_G(g)$):** Local symmetry of a single point.
- **Centralizer of a subgroup ($C_G(H)$):** Measures how much $H$ commutes with the rest of the group.
- **Centralizer of the Center ($C_G(Z(G))$):** Since $Z(G)$ commutes with everything, $C_G(Z(G))=G$.

## How to Solve

Analyzing subgroups and centralizers often requires a systematic approach, especially in finite groups.

### Step-by-Step for Finding a Centralizer
1. **Identify the group operation:** Determine how $xy$ is calculated in your specific group $G$ (e.g., matrix multiplication, permutation composition).
2. **Set up the commuting equation:** For a given element $g$, write the equation $xg=gx$.
3. **Translate into linear or algebraic equations:** If $G$ is a matrix group, write $g$ as a matrix and $x$ as a matrix with variables. Expand the multiplication to obtain a system of equations.
4. **Solve the system:** The solution set for $x$ defines $C_G(g)$.
5. **Verify subgroup properties:** Ensure the resulting set is closed under the operation and includes $e$.

### Example Application: Symmetric Group $S_3$
Let $G=S_3$, the group of permutations of $\\{1, 2, 3\\}$.
- Elements: $\\{id, (12), (13), (23), (123), (132)\\}$.
- To find $C_G((12))$, we check elements $x$ such that $x(12)=(12)x$.
- Clearly $id$ and $(12)$ are in the centralizer.
- Checking $(123)$: $(123)(12) = (13)$ and $(12)(123) = (23)$. Since $(13) \\neq (23)$, $(123) \\notin C_G((12))$.
- Calculation shows $C_G((12)) = \\{id, (12)\\}$.

### Theoretical Analysis via the Class Equation
To check if a group is simple (has no normal subgroups), we use the centralizer index. If $g \\in Z(G)$, its conjugacy class has size 1. If we can show that the sum of the sizes of conjugacy classes (which are indices of centralizers) cannot form the total order of the group without including 1, we can sometimes prove non-simplicity.

## Summary

Subgroups provide the foundational architecture of group theory, allowing for the segmentation of algebraic structures into constituent parts. Centralizers act as a precise diagnostic tool, measuring the commutativity and local symmetry surrounding specific elements or subsets. Through the study of the center $Z(G)$, the class equation, and the properties of centralizers, mathematicians can classify group structures and understand the behavior of symmetry in abstract systems. Mastering these concepts is essential for anyone engaged in higher-level algebra, cryptography, or the physics of symmetry. By combining Lagrange's Theorem, the orbit-stabilizer relationship, and the class equation, one can systematically decompose even complex, non-abelian groups to uncover their underlying order.`;export{e as default};