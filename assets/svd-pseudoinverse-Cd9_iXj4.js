var e=`# Singular Value Decomposition (SVD) and the Pseudoinverse

## Definition

Singular Value Decomposition (SVD) is a fundamental theorem in linear algebra that generalizes the eigendecomposition of a square normal matrix to any $m \\times n$ matrix. Formally, for any real or complex matrix $A \\in \\mathbb{R}^{m \\times n}$, there exists a factorization of the form:

$$A=U\\Sigma V^T$$

In this expression, $U$ is an $m \\times m$ orthogonal matrix whose columns are the left singular vectors of $A$, representing the orthonormal basis of the codomain. $\\Sigma$ is an $m \\times n$ rectangular diagonal matrix with non-negative real numbers $\\sigma_i$ on the diagonal, known as singular values, arranged in descending order. $V$ is an $n \\times n$ orthogonal matrix whose columns are the right singular vectors of $A$, representing the orthonormal basis of the domain.

The Moore-Penrose pseudoinverse, denoted as $A^+$, is a generalization of the matrix inverse for square non-singular matrices to rectangular or singular matrices. It provides a way to solve systems of linear equations $Ax=b$ even when $A$ is not invertible, non-square, or rank-deficient. It is uniquely defined by the four Moore-Penrose conditions:
1. $AA^+A=A$
2. $A^+AA^+=A^+$
3. $(AA^+)^T=AA^+$
4. $(A^+A)^T=A^+A$

## Key Terminology

To understand SVD and its application to the pseudoinverse, one must be familiar with several core concepts:

- **Orthogonal Matrix:** A square matrix $Q$ such that $Q^TQ=QQ^T=I$. These matrices preserve lengths and angles during transformations.
- **Singular Values ($\\sigma_i$):** The square roots of the eigenvalues of the matrix $A^TA$. They quantify the "stretch" factor of the transformation along the principal axes.
- **Rank:** The dimension of the vector space generated by the columns (or rows) of a matrix. A matrix is full rank if $\\text{rank}(A) = \\min(m, n)$.
- **Range (Column Space):** The set of all possible linear combinations of the columns of $A$.
- **Null Space (Kernel):** The set of all vectors $x$ such that $Ax=0$.
- **Least Squares Solution:** The vector $x$ that minimizes the Euclidean norm $\\|Ax-b\\|_2$.

## Purpose

The primary utility of SVD lies in its ability to reveal the intrinsic geometry of a linear transformation. Every linear map $A$ can be decomposed into three distinct geometric phases: a rotation in the domain ($V^T$), a scaling along the principal axes ($\\Sigma$), and a rotation in the codomain ($U$).

The Moore-Penrose pseudoinverse serves several critical purposes in numerical computing and data science:

1. **Solving Underdetermined Systems:** When $A$ has more variables than equations, there are infinitely many solutions. $A^+b$ provides the solution with the minimum $L_2$ norm.
2. **Solving Overdetermined Systems:** When $A$ has more equations than variables (common in regression), the system is often inconsistent. $A^+b$ provides the least squares solution that minimizes $\\|Ax-b\\|_2$.
3. **Data Compression and Denoising:** By truncating the SVD (keeping only the top $k$ singular values), one can approximate the original matrix $A$ while filtering out noise or reducing dimensionality (as in Principal Component Analysis).
4. **Regularization:** SVD allows for the regularization of inverse problems where small singular values lead to numerical instability. By ignoring singular values below a certain threshold, the inversion process becomes stable.

## Fundamental Properties

SVD is mathematically robust because of the following properties:

- **Uniqueness of $\\Sigma$:** While $U$ and $V$ are not unique (due to sign flipping or degenerate singular values), the singular values $\\sigma_i$ are uniquely determined by $A$.
- **Rank Revelation:** The rank of $A$ is exactly the number of non-zero singular values. If $\\sigma_{k+1} = \\dots = \\sigma_n = 0$, then $\\text{rank}(A) = k$.
- **Energy Conservation:** The Frobenius norm of $A$, defined as $\\|A\\|_F = \\sqrt{\\sum \\sum |a_{ij}|^2}$, is equal to the root sum of squares of the singular values: $\\|A\\|_F = \\sqrt{\\sum_{i=1}^r \\sigma_i^2}$.
- **Pseudoinverse construction:** Using the SVD, the pseudoinverse can be explicitly calculated as $A^+ = V\\Sigma^+U^T$, where $\\Sigma^+$ is formed by taking the reciprocal of each non-zero singular value on the diagonal and transposing the result.

## Types & Variations

There are several computational variations of SVD:

- **Full SVD:** Computes $U (m \\times m)$ and $V (n \\times n)$ fully. This is computationally expensive for large matrices.
- **Reduced (Thin) SVD:** If $m > n$, we only need the first $n$ columns of $U$. $U$ becomes $m \\times n$ and $\\Sigma$ becomes $n \\times n$. This is sufficient for most reconstruction tasks.
- **Truncated SVD:** By setting all but the $k$ largest singular values to zero, we obtain the best rank-$k$ approximation of the matrix $A$ in terms of the Frobenius norm (Eckart-Young-Mirsky Theorem).

| Matrix Dimension | SVD Variant | Efficiency Note |
| :--- | :--- | :--- |
| $m \\gg n$ | Thin SVD | Saves memory by ignoring $m-n$ null-space vectors |
| $m \\approx n$ | Full SVD | Necessary for complete unitary basis representation |
| $n \\gg m$ | Truncated SVD | Effective for high-dimensional feature reduction |

The following graph illustrates how a quadratic transformation $f(x) = \\sigma x^2$ scales an input variable $x$. In SVD, the singular value $\\sigma$ acts as a scaling parameter for the transformation along a specific principal component.

\`\`\`graph
0.5*x^2
1.0*x^2
2.0*x^2
\`\`\`

The graph above plots three functions $f(x) = \\sigma x^2$ for $\\sigma \\in \\{0.5, 1.0, 2.0\\}$. Note how the singular value $\\sigma$ dictates the "sharpness" or the magnification factor of the transformation applied to the vector space.

## How to Solve

Calculating the SVD and pseudoinverse for a matrix $A$ manually or algorithmically involves the following analytical steps:

**Step 1: Compute $A^TA$ and $AA^T$**
To find the right singular vectors, solve the eigenvalue problem for $A^TA$. The resulting eigenvectors form the columns of $V$. The square roots of the eigenvalues are the singular values $\\sigma_i$.

**Step 2: Determine $\\Sigma$**
Arrange the singular values $\\sigma_i$ in descending order along the diagonal of an $m \\times n$ matrix $\\Sigma$.

**Step 3: Determine $U$**
For non-zero singular values, use the relation $u_i = \\frac{1}{\\sigma_i}Av_i$. If the rank is less than $m$, use the Gram-Schmidt process to complete the orthonormal basis for $U$.

**Step 4: Construct the Pseudoinverse**
Apply the formula $A^+ = V\\Sigma^+U^T$. The matrix $\\Sigma^+$ is constructed by taking the transpose of $\\Sigma$ and replacing every non-zero entry $\\sigma_i$ with $1/\\sigma_i$.

**Example:**
Consider $A = \\begin{pmatrix} 1 & 1 \\\\ 0 & 0 \\end{pmatrix}$. 
1. $A^TA = \\begin{pmatrix} 1 & 1 \\\\ 1 & 1 \\end{pmatrix}$. Eigenvalues are $\\lambda_1=2, \\lambda_2=0$. Singular values are $\\sigma_1=\\sqrt{2}, \\sigma_2=0$.
2. $\\Sigma = \\begin{pmatrix} \\sqrt{2} & 0 \\\\ 0 & 0 \\end{pmatrix}$.
3. Computing $U$ and $V$ yields the decomposition.
4. $A^+ = \\begin{pmatrix} 0.5 & 0 \\\\ 0.5 & 0 \\end{pmatrix}$.

We can use an interactive graph to explore how changing the singular value $\\sigma$ affects the pseudoinverse of a $1 \\times 1$ matrix. Given $A = [\\sigma]$, the pseudoinverse is $A^+ = [1/\\sigma]$ for $\\sigma \\neq 0$.

\`\`\`interactivegraph
(1/a)*x
params: a=1
range: a=0.1:5
\`\`\`

The interactive graph plots $f(x) = (1/a)x$. You can observe how the slope of the inverse transformation increases as the singular value $a$ approaches zero, highlighting why the pseudoinverse must handle singular values with care during numerical inversion.

## Summary

The Singular Value Decomposition is arguably the most powerful tool in the linear algebra toolbox. By decomposing any transformation into rotations and scalings, it provides an unparalleled view into the structure of linear operators. When applied to solve systems via the Moore-Penrose pseudoinverse, SVD provides a robust mechanism to handle the lack of full rank or square dimensionality. 

Whether one is performing signal processing, implementing machine learning algorithms, or solving differential equations, understanding SVD and the pseudoinverse is essential. It transforms seemingly impossible systems into manageable components, allowing for noise suppression and optimal solutions in underdetermined and overdetermined scenarios alike. The stability provided by truncated SVD ensures that numerical computations remain reliable, even when dealing with matrices that are mathematically singular or physically noisy.`;export{e as default};