var e=`# Nonhomogeneous Linear Systems

A nonhomogeneous linear system represents a cornerstone of linear algebra and differential equations, describing systems where an external forcing function or constant vector drives the behavior of the system, independent of the internal state variables. Unlike homogeneous systems, which describe equilibrium or free motion, nonhomogeneous systems characterize forced responses, control systems, and steady-state displacements in physical structures.

## Definition

A system of linear equations is defined as nonhomogeneous if it can be expressed in the matrix-vector form:

$$A\\mathbf{x} = \\mathbf{b}$$

In this expression, $A$ is an $m \\times n$ coefficient matrix, $\\mathbf{x}$ is the $n \\times 1$ column vector of unknowns, and $\\mathbf{b}$ is an $m \\times 1$ column vector known as the nonhomogeneous term or the forcing function. If $\\mathbf{b} \\neq \\mathbf{0}$, the system is strictly nonhomogeneous. In the context of differential equations, a nonhomogeneous linear system takes the form:

$$\\mathbf{x}'(t) = A(t)\\mathbf{x}(t) + \\mathbf{f}(t)$$

Here, $\\mathbf{x}(t)$ is the state vector, $A(t)$ is the system matrix, and $\\mathbf{f}(t)$ is a non-zero vector-valued function representing the external influence on the system over time. If $\\mathbf{f}(t) = \\mathbf{0}$, the system collapses into its homogeneous counterpart, $\\mathbf{x}'(t) = A(t)\\mathbf{x}(t)$.

## Key Terminology

To understand these systems, one must master the underlying vocabulary of linear algebra:

* **Coefficient Matrix ($A$):** The array of scalars defining the linear relationships between variables.
* **Forcing Term ($\\mathbf{b}$ or $\\mathbf{f}(t)$):** The component that renders the system nonhomogeneous. It represents external inputs like voltage, force, or heat flux.
* **General Solution:** The complete set of all possible solutions to the system, typically expressed as the sum of the complementary solution and a particular solution.
* **Complementary Solution ($\\mathbf{x}_c$):** The solution to the associated homogeneous system ($A\\mathbf{x} = \\mathbf{0}$).
* **Particular Solution ($\\mathbf{x}_p$):** A single, specific solution that satisfies the nonhomogeneous equation $A\\mathbf{x} = \\mathbf{b}$.
* **Kernel (Null Space):** The set of vectors $\\mathbf{x}$ such that $A\\mathbf{x} = \\mathbf{0}$. If the kernel is non-trivial, the system may have infinitely many solutions.
* **Rank:** The dimension of the vector space generated by the columns of $A$, which determines the solvability of the system.

## Purpose

Nonhomogeneous linear systems serve as the mathematical framework for modeling dynamic processes where the system is not isolated. In engineering, they are essential for analyzing how a structure (like a bridge or circuit) responds to external stimuli.

1. **Control Theory:** Determining the necessary input $\\mathbf{f}(t)$ to guide a system's state $\\mathbf{x}(t)$ to a desired target.
2. **Signal Processing:** Modeling the output of a filter given an input signal.
3. **Economics:** Modeling market equilibrium in the presence of external demand shifts.
4. **Physics:** Describing forced oscillations in mechanical systems where external periodic forces are applied.

## Fundamental Properties

The behavior of nonhomogeneous linear systems is governed by the principle of superposition and the structure of the solution space.

### The Principle of Superposition for Nonhomogeneous Systems
If $\\mathbf{x}_{p1}$ is a solution to $A\\mathbf{x} = \\mathbf{b}_1$ and $\\mathbf{x}_{p2}$ is a solution to $A\\mathbf{x} = \\mathbf{b}_2$, then the sum $\\mathbf{x}_{p1} + \\mathbf{x}_{p2}$ is a solution to $A\\mathbf{x} = \\mathbf{b}_1 + \\mathbf{b}_2$. This linearity allows engineers to decompose complex, multifaceted external inputs into simpler, constituent components, solve for each separately, and reconstruct the final response.

### Solution Space Structure
The general solution to the nonhomogeneous equation $A\\mathbf{x} = \\mathbf{b}$ is given by:

$$\\mathbf{x} = \\mathbf{x}_c + \\mathbf{x}_p$$

where $\\mathbf{x}_c$ satisfies $A\\mathbf{x}_c = \\mathbf{0}$ and $\\mathbf{x}_p$ satisfies $A\\mathbf{x}_p = \\mathbf{b}$. This structure implies that the entire set of solutions to a nonhomogeneous system is an "affine space"—essentially a flat subspace (like a plane or line) shifted away from the origin by the vector $\\mathbf{x}_p$.

## Types & Variations

Nonhomogeneous systems can be classified based on the nature of their variables and parameters:

| Category | Description |
| :--- | :--- |
| **Algebraic Systems** | $A\\mathbf{x} = \\mathbf{b}$ where $A$ and $\\mathbf{b}$ are constant. |
| **Differential Systems** | $\\mathbf{x}'(t) = A\\mathbf{x}(t) + \\mathbf{f}(t)$ involving time-varying dynamics. |
| **Consistent Systems** | Systems where at least one solution $\\mathbf{x}$ exists. |
| **Inconsistent Systems** | Systems where $\\mathbf{b}$ lies outside the column space of $A$, resulting in no exact solution. |
| **Overdetermined** | More equations than unknowns ($m > n$), often requiring least-squares methods. |
| **Underdetermined** | Fewer equations than unknowns ($m < n$), often resulting in infinitely many solutions. |

## How to Solve

Solving nonhomogeneous linear systems requires distinct approaches based on whether the system is algebraic or differential.

### Solving Algebraic Systems ($A\\mathbf{x} = \\mathbf{b}$)
1. **Gaussian Elimination:** Transform the augmented matrix $[A|\\mathbf{b}]$ into row-echelon form. Back-substitution then yields the vector $\\mathbf{x}$.
2. **Matrix Inversion:** If $A$ is square and non-singular, $\\mathbf{x} = A^{-1}\\mathbf{b}$. This is computationally expensive for large systems.
3. **LU Decomposition:** Factor $A$ into $L$ (lower triangular) and $U$ (upper triangular) matrices. Solve $L\\mathbf{y} = \\mathbf{b}$ then $U\\mathbf{x} = \\mathbf{y}$.

### Solving Differential Systems ($\\mathbf{x}' = A\\mathbf{x} + \\mathbf{f}(t)$)
1. **Method of Undetermined Coefficients:** If $\\mathbf{f}(t)$ consists of polynomials, exponentials, or sinusoids, assume a solution $\\mathbf{x}_p$ with the same functional form and solve for the unknown coefficients by substituting back into the differential equation.
2. **Variation of Parameters:** A robust method that works for any continuous $\\mathbf{f}(t)$. Given the fundamental matrix $\\Phi(t)$ of the homogeneous system $\\mathbf{x}' = A\\mathbf{x}$, the particular solution is:
   $$\\mathbf{x}_p(t) = \\Phi(t) \\int \\Phi^{-1}(t)\\mathbf{f}(t) \\, dt$$
3. **Laplace Transforms:** Convert the system of differential equations into a system of algebraic equations in the $s$-domain. Solve for $\\mathbf{X}(s)$ and perform an inverse transform to retrieve $\\mathbf{x}(t)$.

### Interactive Example: Forced Oscillation
Consider a simplified forced linear system represented by $x'' + ax = \\sin(t)$. This represents a harmonic oscillator driven by an external sinusoidal force. The parameter $a$ adjusts the natural frequency of the system.

\`\`\`interactivegraph
\\sin(a*x)
params: a=1
range: a=0.1:5
\`\`\`

The graph illustrates the displacement $f(x) = \\sin(ax)$. Notice how varying the parameter $a$ changes the frequency of the oscillation. In a nonhomogeneous system, if the forcing frequency matches the natural frequency of the system ($a$ approaches the resonant frequency), the amplitude of the solution typically grows significantly, demonstrating the importance of understanding the homogeneous component $\\mathbf{x}_c$ when designing physical systems.

## Summary

Nonhomogeneous linear systems are fundamental to our ability to model forced, real-world phenomena. By recognizing that the solution is composed of a homogeneous part (internal dynamics) and a particular part (external response), mathematicians and engineers can decompose complex interactions into manageable segments. Whether through Gaussian elimination for static algebraic systems or the method of variation of parameters for time-dependent differential systems, these tools remain indispensable for analytical progress in science, economics, and engineering. The core requirement for any analysis is identifying the forcing function's influence relative to the system's inherent constraints defined by its coefficient matrix.`;export{e as default};